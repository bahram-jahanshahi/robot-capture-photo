## How to run the capture photo by webcam
```shell
uvicorn capture_photo_by_webcam:app --host 127.0.0.1 --port 8000
```

## Library needed to be installed for VAD_Sidecar
```shell
pip3 install webrtcvad sounddevice numpy requests flask
```
# robot-capture-photo
